@model List<OffshoreTrack.Models.Rateio>
@{
    ViewData["Title"] = "Index";
}

<div class="pagetitle">
    <div class="row">
        <div class="col-md-6">
            <h1 class="display-4">Rateios</h1>
        </div>
        <div class="col-md-6">
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a class="btn btn-primary" role="button" asp-area="" asp-controller="Rateio" asp-action="New"> Cadastrar novo </a>
            </div>
        </div>
    </div>
</div>


<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <br />
            <div class="card-body">
                <table class="table table-striped" id="tableRateio">
                    <thead>
                        <tr>
                            <th scope="col">Rateio</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.rateio</td>
                                <td>
                                    <a class="btn btn-primary" href="Rateio/Read/@item.id_rateio"><i class="bi bi-eye"></i></a>
                                    <a class="btn btn-success" href="Rateio/Edit/@item.id_rateio"><i class="bi bi-pencil"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#tableRateio').DataTable();
        });
    </script>
    }
